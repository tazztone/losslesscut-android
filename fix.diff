--- core/domain/src/main/java/com/tazztone/losslesscut/domain/usecase/SilenceDetectionUseCase.kt
+++ core/domain/src/main/java/com/tazztone/losslesscut/domain/usecase/SilenceDetectionUseCase.kt
@@ -110,14 +110,8 @@
             if (silEnd <= cursor) continue

             // Fix boundary tiny gaps: if silence starts almost at 0, pull it to 0
-            val effectiveStart = when {
-                silStart < BOUNDARY_THRESHOLD_MS -> 0L
-                clipEnd - silStart < BOUNDARY_THRESHOLD_MS -> clipEnd
-                else -> silStart
-            }
-            val effectiveEnd = when {
-                silEnd < BOUNDARY_THRESHOLD_MS -> 0L
-                clipEnd - silEnd < BOUNDARY_THRESHOLD_MS -> clipEnd
-                else -> silEnd
-            }
+            val effectiveStart = clampBoundary(silStart, clipEnd)
+            val effectiveEnd = clampBoundary(silEnd, clipEnd)

             if (effectiveStart > cursor) {
                 rawSegments.add(TrimSegment(UUID.randomUUID(), cursor, effectiveStart, SegmentAction.KEEP))
@@ -176,6 +170,12 @@
         return result
     }

+    private fun clampBoundary(value: Long, clipEnd: Long): Long = when {
+        value < BOUNDARY_THRESHOLD_MS -> 0L
+        clipEnd - value < BOUNDARY_THRESHOLD_MS -> clipEnd
+        else -> value
+    }
+
     private companion object {
         private const val BOUNDARY_THRESHOLD_MS = 10L
         private const val US_PER_MS = 1000L
